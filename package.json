{
  "name": "electric-mgmt",
  "version": "1.0.0",
  "description": "전기설비 관리 시스템",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
