# 변경사항 테스트 체크리스트

## 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `CLAUDE.md` | 프로젝트 문서 전면 재작성 (기능 영향 없음) |
| `backend/server.js` | `/api/legal-inspections`, `/api/fault-history` 라우트 마운트 추가 |
| `frontend/src/App.jsx` | 법정검사 알림 배너·뱃지, 이상 이력 탭, 알림 실시간 갱신 추가 |
| `frontend/src/pages/LegalInspectionPage.jsx` | `onSaved` prop 수신 → 저장 시 App 알림 갱신 콜백 호출 |

---

## 테스트 항목

### 1. API 라우트 정상 응답

```bash
curl http://localhost:4000/api/legal-inspections
# → { "success": true, "data": [...] }

curl http://localhost:4000/api/fault-history
# → { "success": true, "data": [...] }
```

### 2. 법정검사 알림 배너 (App.jsx)

- [ ] 앱 시작 시 만료 초과 항목 → 상단 빨간 배너 표시
- [ ] 앱 시작 시 D-30 이내 항목 → 상단 노란 배너 표시
- [ ] 배너 클릭 시 법정검사 탭으로 이동
- [ ] 법정검사 탭 버튼에 임박·초과 건수 뱃지(빨간 원) 표시

### 3. 법정검사 저장 시 알림 즉시 갱신 (핵심 수정사항)

- [ ] 법정검사 탭에서 신규 항목 등록 → 배너·뱃지가 새로고침 없이 즉시 갱신
- [ ] 만료일 수정(D-30 이내 ↔ 이후 변경) → 배너·뱃지 즉시 반영
- [ ] 기존 항목 수정 저장 → 알림 갱신 확인

### 4. 이상 이력 탭

- [ ] ⚠️ 이상 이력 탭 노출 (점검 이력과 법정검사 사이)
- [ ] 요약 카드: 전체 / 미처리 / 심각 건수 표시
- [ ] 미처리 > 0 이면 카드 테두리 주황, 심각 > 0 이면 빨간 테두리
- [ ] 이상 이력 등록: 설비·발생일·이상내용 필수 유효성 검사
- [ ] 등록 후 요약 카드 및 목록 즉시 갱신
- [ ] 수정 폼 진입 시 기존 데이터 자동 입력, 설비 선택 비활성화
- [ ] 처리완료 체크 후 저장 → 처리상태 배지 변경(미처리→처리완료)
- [ ] 심각도 필터 / 처리상태 필터 / 기간 필터 동작

### 5. 기존 기능 회귀 확인

- [ ] 설비 관리 탭 정상 동작
- [ ] 일상점검 입력·수정 정상 동작
- [ ] 점검 이력 탭 → 수정 클릭 시 일상점검 탭으로 이동 및 폼 자동 입력
- [ ] 법정검사 목록·등록·수정 정상 동작
